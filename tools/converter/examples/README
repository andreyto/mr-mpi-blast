Examples of converter utilities

Run "test.sh"

    This reads all binary hit files (./hits/*.bin) generated from a
    mr-mpi-blast run and convert the binary files into CSV, SQLite
    database, and HDF5 database files and save the files at "./converted"
    directory.
    

