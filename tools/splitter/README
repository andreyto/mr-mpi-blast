Util to split sequences in the input FASTA file into 'queryLen' bp length 
sub sequences with 'overlapBP' overlapped.

    USAGE: python splitter.py fasta_file queryLen overlapBP outfile

    EXAMPLE:

        $python splitter.py infile 1000 100 outfile
